{
  "name": "ecodomehomes",
  "version": "1.0.0",
  "private": true,
  "description": "EcoDomeHomes marketing site (static HTML) plus Vercel serverless lead-capture functions.",
  "scripts": {
    "test:leads": "node api/_lib/leads.test.js",
    "check:locales": "node scripts/check-locale-parity.mjs",
    "check:pricing": "node scripts/check-pricing-parity.mjs",
    "check:sitemap": "node scripts/gen-sitemap.mjs --check",
    "check:schema": "node scripts/gen-schema.mjs --check",
    "check:images": "node scripts/fix-image-markup.mjs --check",
    "check:descriptions": "node scripts/trim-descriptions.mjs --check"
  },
  "dependencies": {
    "nodemailer": "^6.9.16"
  },
  "devDependencies": {
    "sharp": "^0.35.3"
  }
}
